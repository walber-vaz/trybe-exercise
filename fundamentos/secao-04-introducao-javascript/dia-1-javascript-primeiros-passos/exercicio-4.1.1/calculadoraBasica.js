const a = 5;
const b = 5;

const som = a + b;
const sub = a - b;
const mult = a * b;
const div = a / b;
const mod = a % b;

console.log(`A soma entre ${a} + ${b} => ${som}`);
console.log(`A subtração entre ${a} - ${b} => ${sub}`);
console.log(`A multiplicação entre ${a} * ${b} => ${mult}`);
console.log(`A divisão entre ${a} / ${b} => ${div}`);
console.log(`O resto da divisão entre ${a} % ${b} => ${mod}`);
